/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package today;

/**
 *
 * @author Kanishka
 */
public class SQLiteImageTest {

    /**
     * @param args the command line arguments
     */
    public static void dispCard() {
        // TODO code application logic here
        DemoFrameCard df=new DemoFrameCard();
        df.setLocationRelativeTo(null);
        df.setVisible(true);
    }
    
    public static void dispPass() {
        // TODO code application logic here
        DemoFramePass df=new DemoFramePass();
        df.setLocationRelativeTo(null);
        df.setVisible(true);
    }
    
        public static void dispPhoto() {
        // TODO code application logic here
        DemoFramePhoto df=new DemoFramePhoto();
        df.setLocationRelativeTo(null);
        df.setVisible(true);
    }
    
    public static void save() {
        // TODO code application logic here
        DemoFrameCard df=new DemoFrameCard();
        df.setLocationRelativeTo(null);
        df.setVisible(true);
    }
}
